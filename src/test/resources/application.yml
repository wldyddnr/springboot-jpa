spring:
  #설정을 다 빼도 메모리모드로 실행(다 없어도됨)
#  datasource:
#    url: jdbc:h2:mem:test; # https://www.h2database.com/html/cheatSheet.html in-memory
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        # show_sql: true
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace #스프링 부트 2.x, hibernate5
#  org.hibernate.orm.jdbc.bind: trace #스프링 부트 3.x, hibernate6